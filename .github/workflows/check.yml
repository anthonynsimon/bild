name: Check

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  check:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-15
        # 1.25 is the floor imposed by golang.org/x/image; keep in sync with go.mod.
        go-version:
          - '1.25'
          - '1.26'

    steps:
      - uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
          # Two deps only; the matrix shares one cache key and the restores race.
          cache: false

      - name: Build
        run: make build

      - name: Test
        run: make test
